/**
 * Analytics + Health Score module — read-only derivations for the dashboard, and the health score.
 * Owns no source data. Implements {@code design-docs/FINANCIAL_RULES.md} and
 * {@code design-docs/HEALTH_SCORE_SPEC.md}; does not invent logic.
 */
package com.rohit.nyvra.analytics;
