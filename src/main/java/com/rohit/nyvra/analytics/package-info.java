/**
 * Analytics + Health Score module — read-only derivations for the dashboard, and the health score.
 * Owns no source data. Implements {@code docs/product/FINANCIAL_RULES.md} and
 * {@code docs/product/HEALTH_SCORE_SPEC.md}; does not invent logic.
 */
package com.rohit.nyvra.analytics;
