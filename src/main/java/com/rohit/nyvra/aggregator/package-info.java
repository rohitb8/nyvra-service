/**
 * Aggregator module — the internal interface the rest of the app uses to talk to the Account
 * Aggregator ecosystem (consent request/approval, FIU-FIP communication). Isolates AA-framework
 * specifics behind a clean boundary.
 */
package com.rohit.nyvra.aggregator;
